{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "env": {
      "DATABASE_URL": "your-database-url"
    },
    "build": {
      "env": {
        "DATABASE_URL": "your-database-url"
      },
      "commands": [
        "npx prisma generate"
      ]
    }
  }
  